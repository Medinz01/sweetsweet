import { slugify } from 'slugify'; // or whatever utility function you have
// Dummy test just to establish the framework works

describe('Dummy Unit Setup', () => {
  it('should equal true', () => {
    expect(true).toBe(true);
  });
  
  it('should do basic math', () => {
    expect(1 + 1).toEqual(2);
  });
});
